# T1DiabetesGranada xLSTM 30-min BG prediction
# Python 3.12 (conda env: py312_cu121, CUDA 12.x)
# Pin numpy<2.3 to avoid shap/numba × numpy 2.3 conflict.

# ----- Core numerical / data -----
numpy<2.3
numba>=0.61.2
pandas>=2.2
scipy>=1.11
scikit-learn>=1.5

# ----- Deep learning (training / inference / SHAP) -----
torch>=2.4
xlstm==2.0.5

# ----- Hyperparameter search & tracking -----
optuna>=3.5
optuna-dashboard>=0.15      # web UI for xlstm_search.db
sqlalchemy>=2.0             # required by Optuna's SQLite RDB storage
mlflow>=2.10

# ----- Model interpretation -----
shap>=0.45

# ----- Plotting (SHAP plots + training curves) -----
matplotlib>=3.8

# ----- Utilities -----
tqdm>=4.66
pyyaml>=6.0                 # mlflow / optuna config loading
cloudpickle>=3.0            # mlflow pytorch model serialization
joblib>=1.3                 # scaler pickling helpers

# ----- Testing -----
pytest>=8.0
